{
  "name": "visa-scraper",
  "version": "1.0.0",
  "description": "Bot to scrape for visa appointments from TLS Contact website",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "node build/api.ts"
  },
  "author": "Matthew Chuong",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^22.6.2"
  },
  "devDependencies": {
    "@types/node": "^20.12.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
